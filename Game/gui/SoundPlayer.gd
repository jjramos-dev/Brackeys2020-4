extends Node


onready var intro  = $intro
onready var bg = $bg
onready var hit_shield = $hit_shield
onready var enemy_walk = $enemy_walk
onready var hit_wall = $hit_wall
onready var hit_player = $hit_player
onready var jump = $jump
onready var charge = $charge
onready var enemy_die = $enemy_die
onready var player_die = $player_die
onready var fall = $fall

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta: float) -> void:
#	pass



